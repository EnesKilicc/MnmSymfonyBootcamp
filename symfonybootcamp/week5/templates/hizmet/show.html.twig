{% extends 'base.html.twig' %}

{% block title %}Hizmet{% endblock %}

{% block body %}
    <h1>Hizmet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hizmet.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ hizmet.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ hizmet.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hizmet_index') }}">back to list</a>

    <a href="{{ path('hizmet_edit', {'id': hizmet.id}) }}">edit</a>

    {{ include('hizmet/_delete_form.html.twig') }}
{% endblock %}
